# API Configuration
# In development: Vite proxy forwards /api/* requests to https://localhost:5165
# In production: Frontend and backend served from same origin, requests go to /api directly
VITE_API_BASE=/api
